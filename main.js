window.onload = function() {
    var elem = document.getElementById("plotCanvas");
    add_click_elements(elem, 'image.png', 'regions.json');
}

function add_click_elements(elem, image_url, regions_url) {
    console.log('Adding click elements to ', elem);
}
